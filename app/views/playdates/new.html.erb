<p>Create a new play date: </p>

<%= form_for(@playdate) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :location %>
  <%= f.text_field :location %>
  <% @family.children.each do |child| %>
  <%= f.label :participants, "Select a Child" %>
  <%= f.select :participants, options_from_collection_for_select(@family.children, :id, :name) %></br>
<%= f.submit %>

  <%end%>
<%end%>
