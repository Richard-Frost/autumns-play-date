<h1> Activate Account </h1>

<%= form_for @user, :url => user_activation_path(params[:id]) do |f| %>
  <%= f.label :password %>
  <%= f.password_field :password %>
<%= f.submit "Update Password" %>
<%end%>